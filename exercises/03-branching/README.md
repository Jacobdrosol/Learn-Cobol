# Exercise 03: Branching and Input Files

Goal: read a line sequential file, parse CSV-like records, use `IF`, and
calculate totals.

Run:

```bash
make grades
```

Practice changes:

- Add another learner to `grades.dat`.
- Change the pass threshold.
- Add an `EVALUATE` statement that maps scores to letter grades.
