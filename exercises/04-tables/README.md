# Exercise 04: Tables

Goal: use `OCCURS`, indexes, and a simple search over in-memory data.

Run:

```bash
make tables
```

Practice changes:

- Add another account.
- Change the lookup account.
- Add a total balance calculation.
