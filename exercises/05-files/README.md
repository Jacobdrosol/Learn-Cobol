# Exercise 05: File Processing

Goal: read business records from a text file and produce a report.

Run:

```bash
make files
```

Practice changes:

- Add a customer record.
- Change the threshold for "large" customers.
- Split the parsing paragraph into a reusable subprogram.
